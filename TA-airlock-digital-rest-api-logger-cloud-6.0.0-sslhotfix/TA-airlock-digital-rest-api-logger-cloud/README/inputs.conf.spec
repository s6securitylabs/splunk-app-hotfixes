[airlock_policies://<name>]
local_airlock_cloud_directoryid = [Optional] Will override the addon configuration page. Used for multi-tenancy cloud instances.
local_airlock_cloud_tenantid = [Optional] Will override the addon configuration page. Used for multi-tenancy cloud instances.

[airlock_agent://<name>]
local_airlock_cloud_directoryid = [Optional] Will override the addon configuration page. Used for multi-tenancy cloud instances.
local_airlock_cloud_tenantid = [Optional] Will override the addon configuration page. Used for multi-tenancy cloud instances.

[airlock_execution_history://<name>]
execution_types_to_collect = 
delete_existing_checkpoint = When checked will delete the existing checkpoint stored in Splunk and exit. This used for troubleshooting purposes only and the utility will not index events while this option is selected.
local_airlock_cloud_directoryid = [Optional] Will override the addon configuration page. Used for multi-tenancy cloud instances.
local_airlock_cloud_tenantid = [Optional] Will override the addon configuration page. Used for multi-tenancy cloud instances.

[airlock_server_activities://<name>]
delete_existing_checkpoint = When checked will delete the existing checkpoint stored in Splunk and exit. This used for troubleshooting purposes only and the utility will not index events while this option is selected.
local_airlock_cloud_directoryid = [Optional] Will override the addon configuration page. Used for multi-tenancy cloud instances.
local_airlock_cloud_tenantid = [Optional] Will override the addon configuration page. Used for multi-tenancy cloud instances.
[airlock_otp_usage://<name>]
delete_existing_checkpoint = When checked will delete the existing OTP usage state checkpoint stored in Splunk and exit. This used for troubleshooting purposes only and the utility will not index events while this option is selected.
local_airlock_cloud_directoryid = [Optional] Will override the addon configuration page. Used for multi-tenancy cloud instances.
local_airlock_cloud_tenantid = [Optional] Will override the addon configuration page. Used for multi-tenancy cloud instances.
